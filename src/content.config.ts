import {defineCollection} from 'astro:content'; import {z} from 'astro/zod'; import {glob} from 'astro/loaders';
const locale=z.enum(['en','es']); const experience=defineCollection({loader:glob({pattern:'**/*.md',base:'./src/content/experience'}),schema:z.object({locale,company:z.string(),role:z.string(),startDate:z.date(),endDate:z.date().optional(),location:z.string(),employmentType:z.string().optional(),summary:z.string(),highlights:z.array(z.string()),technologies:z.array(z.string())})}); const projects=defineCollection({loader:glob({pattern:'**/*.md',base:'./src/content/projects'}),schema:z.object({locale,title:z.string(),summary:z.string(),problem:z.string(),solution:z.string(),result:z.string().optional(),technologies:z.array(z.string()),repository:z.string().optional(),demo:z.string().optional(),featured:z.boolean().optional().default(false)})}); export const collections={experience,projects};