---
import { useTranslations, type Locale } from '../i18n/ui';

interface Props {
  title: string;
  problem: string;
  solution: string;
  result?: string;
  technologies: string[];
  repository?: string;
  demo?: string;
}

const { title, problem, solution, result, technologies, repository, demo } =
  Astro.props;
const locale = (Astro.currentLocale ?? 'en') as Locale;
const t = useTranslations(locale);
---

<article
  data-project-accordion-item
  data-open="false"
  class="project-accordion group flex flex-col gap-3"
>
  <!-- prettier-ignore -->
  <button
    type="button"
    data-project-trigger
    aria-expanded="false"
    class="project-trigger 
    flex w-full items-center justify-between
    border-b-2 border-gray-200 bg-inherit
    px-5 py-5 text-left 
    group-[.is-open]:border-blue-200 sm:px-6 
    dark:border-gray-800 dark:bg-inherit dark:group-[.is-open]:border-blue-500/60 dark:hover:border-blue-500/60"
  >
    <div class="min-w-0 ">
      <h3
        class="text-lg font-bold text-gray-900 transition-colors duration-200 group-hover:text-blue-700 sm:text-xl dark:text-gray-100 dark:group-hover:text-blue-300"
      >
        {title}
      </h3>
    </div>

    <span class="flex h-10 w-10 shrink-0 items-center justify-center">
      <svg
        data-project-icon
        aria-hidden="true"
        viewBox="0 0 20 20"
        fill="none"
        stroke="currentColor"
        stroke-width="1.8"
        class="h-5 w-5 transition-transform duration-300 ease-out"
      >
        <path
          d="M5 7.5L10 12.5L15 7.5"
          stroke-linecap="round"
          stroke-linejoin="round"></path>
      </svg>
    </span>
  </button>

  <div data-project-panel class="accordion-panel is-closed">
    <div class="project-panel overflow-hidden shadow-sm sm:px-6">
      <div class="px-5 pt-5 pb-6 sm:px-0">
        <div class="mb-6 grid grid-cols-1 gap-6 sm:grid-cols-2">
          <div>
            <h4 class="mb-2 font-semibold text-gray-900 dark:text-gray-100">
              {t.projects.problem}
            </h4>
            <p class="leading-relaxed text-gray-700 dark:text-gray-300">
              {problem}
            </p>
          </div>
          <div>
            <h4 class="mb-2 font-semibold text-gray-900 dark:text-gray-100">
              {t.projects.solution}
            </h4>
            <p class="leading-relaxed text-gray-700 dark:text-gray-300">
              {solution}
            </p>
          </div>
        </div>

        {
          result && (
            <div class="mb-6">
              <h4 class="mb-2 font-semibold text-gray-900 dark:text-gray-100">
                {t.projects.result}
              </h4>
              <p class="leading-relaxed text-gray-700 dark:text-gray-300">
                {result}
              </p>
            </div>
          )
        }

        <div class="mb-4 flex flex-wrap gap-2">
          {
            technologies.map((tech) => (
              <span class="inline-flex rounded bg-blue-50 px-3 py-1 text-sm font-medium text-blue-700 dark:bg-blue-950 dark:text-blue-300">
                {tech}
              </span>
            ))
          }
        </div>

        {
          (repository || demo) && (
            <div class="flex flex-wrap gap-4 border-t border-gray-200 pt-4 dark:border-gray-800">
              {repository && (
                <a
                  href={repository}
                  target="_blank"
                  rel="noopener noreferrer"
                  class="text-blue-600 transition-colors hover:text-blue-700 dark:text-blue-400 dark:hover:text-blue-300"
                >
                  Repository →
                </a>
              )}
              {demo && (
                <a
                  href={demo}
                  target="_blank"
                  rel="noopener noreferrer"
                  class="text-blue-600 transition-colors hover:text-blue-700 dark:text-blue-400 dark:hover:text-blue-300"
                >
                  Demo →
                </a>
              )}
            </div>
          )
        }
      </div>
    </div>
  </div>
</article>

<style>
  .accordion-panel {
    overflow: hidden;
    max-height: 0;
    opacity: 0;
    transform: translateY(-0.5rem);
    transition: all 0.3s cubic-bezier(0, 0, 0.3, 1) !important;
  }

  .accordion-panel.is-open {
    max-height: 2000px;
    opacity: 1;
    transform: translateY(0);
    transition: all 0.3s cubic-bezier(0.7, 0, 0.3, 1) !important;
  }
</style>
